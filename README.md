# email-writer-ai
AI-powered Email Reply Generator built with Spring Boot and a Chrome Extension. Adds an “AI Reply” button inside Gmail to generate smart responses instantly. Reads email content, sends it to a Spring Boot backend, and inserts the AI-generated reply directly into the compose box.
